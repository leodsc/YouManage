import { WageCurrencyPipe } from './wage-currency.pipe';

describe('WageCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new WageCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
