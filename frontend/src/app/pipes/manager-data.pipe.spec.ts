import { ManagerDataPipe } from './manager-data.pipe';

describe('ManagerDataPipe', () => {
  it('create an instance', () => {
    const pipe = new ManagerDataPipe();
    expect(pipe).toBeTruthy();
  });
});
