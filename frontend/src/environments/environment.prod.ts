export const environment: {
  production: true;
  currentIconOpen: any;
  token: string;
  resolution: number;
  server: string;
} = {
  production: true,
  currentIconOpen: '',
  token: '',
  resolution: window.innerWidth,
  server: 'http://localhost:8080/',
};
